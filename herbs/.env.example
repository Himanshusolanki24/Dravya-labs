# ─── Herb Knowledge Microservice ───────────────────────────
# API key for authenticating requests from the AI backend
HERB_API_KEY=your-secret-api-key-here

# Directory containing trained model artifacts
# (herb_model.pth, model_metadata.json, herb_lookup.csv)
MODEL_DIR=./model

# Server port
PORT=8002
